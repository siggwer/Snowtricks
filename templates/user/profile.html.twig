{% extends "base.html.twig" %}

{% block title %}Snowtrick / Mon profil{% endblock %}

{% block body %}

    <div class="container my-5">
        <div class="row">
            <div class="col-12">
                <div class="bg-light update-avatar">
                    <div class="card">
                        <div class="card-body">
                            <h3>Modifier ton  avatar {{ user.username }}</h3>
                            <img src="{{ asset(avatar.path) }}" class="img-circle" alt="">
                            {{ include("user/_form_profile.html.twig", { form: form }) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}